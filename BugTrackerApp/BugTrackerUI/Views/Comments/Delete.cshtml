@model BugTrackerApp.Comment

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Comment</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Issue.IssueTitle)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Issue.IssueTitle)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.CommentBody)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.CommentBody)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.CommentTime)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.CommentTime)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "Index", new { id = Model.IssueId }, null)
        </div>
    }
</div>
